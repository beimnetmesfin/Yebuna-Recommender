[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"

[project]
name="yebuna recommender"
version="0.1.0"
description="content-based recommendation system for yebuna"
requires-python=">3.14"
readme="README.md"

[tool.pytest.ini_options]
pythonpath=["."]
testpaths=["tests"]